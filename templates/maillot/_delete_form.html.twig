<form method="post" action="{{ path('app_maillot_delete', {'id': maillot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maillot.id) }}">
    <button class="btn">Delete</button>
</form>
