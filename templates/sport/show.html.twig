{% extends 'base.html.twig' %}

{% block title %}Sport{% endblock %}

{% block body %}
    <h1>Sport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sport.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ sport.libelle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sport.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info"href="{{ path('app_sport_index') }}">Revenir à la liste des Sports</a>

    <a class="btn btn-warning"href="{{ path('app_sport_edit', {'id': sport.id}) }}">Modifier le Sport</a>

    {{ include('sport/_delete_form.html.twig') }}
{% endblock %}
