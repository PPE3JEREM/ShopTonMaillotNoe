{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ equipe.libelle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ equipe.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info"href="{{ path('app_equipe_index') }}">Revenir à la liste des Équipes</a>

    <a class="btn btn-warning"href="{{ path('app_equipe_edit', {'id': equipe.id}) }}">Modifier l'équipe</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
